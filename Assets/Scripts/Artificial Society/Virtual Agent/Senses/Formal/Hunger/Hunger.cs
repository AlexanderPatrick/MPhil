using UnityEngine;
using System.Collections;

namespace Senses {
    // Breaking away from the problems that interoception might cause with there being no common behaviour between the senses other than they react to internal stimuli.
    public class Hunger : MonoBehaviour {
        // Use this for initialization
        void Start() {

        }

        // Update is called once per frame
        void Update() {

        }
    }
}